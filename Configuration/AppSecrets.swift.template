//
//  AppSecrets.swift.template
//  AppStarter
//
//  INSTRUCTIONS:
//  1. Copy this file to AppSecrets.swift (same directory)
//  2. Replace placeholder values with your actual API keys
//  3. AppSecrets.swift is git-ignored and will not be committed
//
//  IMPORTANT: Never commit actual API keys to version control!
//

import Foundation

/// App secrets and API keys
/// CUSTOMIZE: Add your actual API keys here
enum AppSecrets {
    
    // MARK: - RevenueCat
    
    /// RevenueCat API key
    /// Get from: https://app.revenuecat.com
    static let revenueCatAPIKey = "YOUR_REVENUECAT_API_KEY"
    
    // MARK: - Analytics
    
    /// Firebase configuration file
    /// Add GoogleService-Info.plist to project (also git-ignored)
    
    /// Mixpanel project token
    /// Get from: https://mixpanel.com/project/YOUR_PROJECT/settings
    static let mixpanelToken = "YOUR_MIXPANEL_TOKEN"
    
    // MARK: - Backend API
    
    /// API authentication token (if needed)
    static let apiAuthToken = "YOUR_API_AUTH_TOKEN"
    
    /// API secret key (if needed)
    static let apiSecretKey = "YOUR_API_SECRET_KEY"
    
    // MARK: - Third-Party Services
    
    /// Add other service API keys as needed
    // static let serviceAPIKey = "YOUR_SERVICE_API_KEY"
}

// MARK: - Usage

/*
 
 USAGE IN CODE:
 
 ```swift
 // RevenueCat
 Purchases.configure(withAPIKey: AppSecrets.revenueCatAPIKey)
 
 // Mixpanel
 Mixpanel.initialize(token: AppSecrets.mixpanelToken)
 
 // API requests
 let headers = ["Authorization": "Bearer \(AppSecrets.apiAuthToken)"]
 ```
 
 SECURITY NOTES:
 
 1. This file (AppSecrets.swift) is git-ignored
 2. Never commit actual API keys
 3. Use environment variables in CI/CD
 4. Rotate keys if accidentally exposed
 5. Use different keys for debug/release builds if possible
 
 SETUP FOR NEW TEAM MEMBERS:
 
 1. Copy AppSecrets.swift.template to AppSecrets.swift
 2. Request API keys from team lead
 3. Add keys to AppSecrets.swift
 4. Build and run
 
 */
